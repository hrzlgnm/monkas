name: "CI"

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
  # measure against bitrot
  # also rotates docker images monthly, by pushing new docker images
  # the rotation will happen by renovate
  schedule:
    - cron: "0 0 1 * *"

env:
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}

concurrency:
  # On main, we want all builds to complete even if merging happens faster to make it easier to discover at which point something broke.
  group: ${{ github.ref == 'refs/heads/main' && format('ci-main-{0}', github.sha) || format('ci-{0}', github.ref) }}
  cancel-in-progress: true

jobs:
  changes:
    name: 🔍 Detect Changes
    runs-on: ubuntu-slim
    outputs:
      branch: ${{ steps.branch.outputs.branch }}
      cmake: ${{ steps.filter.outputs.cmake }}
      code: ${{ steps.filter.outputs.code }}
      docker: ${{ steps.filter.outputs.docker }}
      lint: ${{ steps.filter.outputs.lint }}
      tests: ${{ steps.filter.outputs.tests }}
    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v6

      - name: 🔍 Check for changes
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        id: filter
        with:
          filters: |
            docker:
              - '.github/docker/**'
            code:
              - '.clang-tidy'
              - '.github/workflows/build-and-test-reusable.yml'
              - '.github/workflows/static-analysis-reusable.yml'
              - 'examples/**/*.cpp'
              - 'external/**/*.cpp'
              - 'external/**/*.h'
              - 'include/**/*.hpp'
              - 'src/**/*.hpp'
              - 'src/**/*.cpp'
              - '!src/**/*.test.cpp'
            cmake:
              - '.gersemirc'
              - '**/CMakeLists.txt'
            lint:
              - '.clang-format'
              - '.github/workflows/lint-reusable.yml'
            tests:
              - 'src/**/*.test.cpp'

      - name: Output branch name
        id: branch
        run: |
          echo "branch=${{ env.BRANCH_NAME }}" >> $GITHUB_OUTPUT

  typos:
    name: 📝 Check typos
    if: always()
    uses: hrzlgnm/actions/.github/workflows/typos-reusable.yml@acb5bddd856e0fd20697dc5f3f90ccddd813d8f0 # v1.3.0

  lint:
    name: Lint
    needs: changes
    if: needs.changes.outputs.code == 'true' || needs.changes.outputs.lint == 'true'
    uses: ./.github/workflows/lint-reusable.yml

  gersemi:
    name: 📐 Check CMake format
    needs: changes
    if: needs.changes.outputs.cmake == 'true'
    uses: hrzlgnm/actions/.github/workflows/gersemi-reusable.yml@acb5bddd856e0fd20697dc5f3f90ccddd813d8f0 # v1.3.0

  build-and-test:
    name: Build and Test
    permissions:
      pull-requests: write # retain for PR operations
      checks: write # required for action-junit-report annotations
    needs: changes
    if: needs.changes.outputs.code == 'true' || needs.changes.outputs.tests == 'true' || needs.changes.outputs.cmake == 'true'
    uses: ./.github/workflows/build-and-test-reusable.yml

  docker:
    name: "🐳"
    needs: changes
    if: needs.changes.outputs.docker == 'true'
    permissions:
      contents: read
      packages: write # Needed to push to GHCR

    uses: hrzlgnm/actions/.github/workflows/docker-reusable.yml@acb5bddd856e0fd20697dc5f3f90ccddd813d8f0 # v1.3.0
    with:
      branch: ${{ needs.changes.outputs.branch }}

  static-analysis:
    name: Static Analysis
    needs: changes
    if: needs.changes.outputs.code == 'true' || needs.changes.outputs.tests == 'true'
    uses: ./.github/workflows/static-analysis-reusable.yml

  all-required-green: # This job does nothing and is only used for the branch protection
    name: CI Status
    runs-on: ubuntu-latest
    needs:
      - build-and-test
      - docker
      - gersemi
      - lint
      - typos
      - static-analysis
    if: always()
    steps:
      - name: Check whether the needed jobs succeeded or failed
        uses: re-actors/alls-green@2765efec08f0fd63e83ad900f5fd75646be69ff6
        with:
          allowed-skips: >-
            build-and-test,
            docker,
            gersemi,
            lint,
            static-analysis
          jobs: ${{ toJSON(needs) }}
